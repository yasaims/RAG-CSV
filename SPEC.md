# SPEC.md

ここには機能要件及び確定した仕様を記載する
**最新の要件or仕様と一致させる。削除された仕様の説明や変更ログを残さないこと。**

# フェーズ共通

## フェーズ定義

- **フェーズ1**: 単一ホスト（ローカル PC / GitHub Codespaces）で完結する軽量実装。
  外部のマネージドサービス・課金を伴う API に依存しない
- **フェーズ2**: AWS クラウド上で運用する構成。フェーズ1 の実装を実行基盤へ載せ替える

フェーズ2 の各章は、フェーズ1 との差分（追加で満たすべき要件）を記載する。
フェーズ1 の記載のうちフェーズ2 で置換されるものには「（フェーズ2 で置換）」と
明記する。それ以外の記載はフェーズ2 でも有効なまま引き継がれる。

## 機能要件

- 自然言語テキストを受け付け、事故記録データを参照した回答を返すAPIサーバーを提供する
- 日本語による入出力
- 回答は参照した事故記録の `id` を伴い、元の CSV 行まで常に一意に辿れること

## データ

- CSV形式。ある工場における事故・ヒヤリハットの記録
- 列は `id, date, time, location, equipment, severity, worker_id, worker_role, worker_experience_years, description, cause, countermeasure`
- severity は「ヒヤリハット / 軽微 / 中位 / 重大」の 4 段階
- description / cause / countermeasure は日本語の自由記述
- cause / countermeasure は空の場合あり

## API 仕様

- バージョニングは URL パス方式（`/v1/...`）。破壊的変更のときのみメジャー番号を上げる
- 自然言語での問い合わせは「質問 + 回答」を 1 リソースとして扱う
  - `POST /v1/queries` — 質問を受け付け、回答を生成して `201 Created` で返す（`Location` ヘッダーに参照 URL）
  - `GET /v1/queries/{id}` — 過去の問い合わせを再参照する
  - レスポンスは `id`（UUIDv7）, `question`, `answer`, `citations`, `basis`, `created_at` を含む
- `citations` は参照した事故記録を表す配列（キーとして必須。該当する記録がない場合は空配列）
  - 記述検索の回答: ヒットした記録
  - 集計の回答: 集計対象になった母集団の全記録（内訳の集計根拠を検証できるようにするため）
  - 該当なし、絞り込み条件が実データに存在しない、または指定された期間がデータの
    収録期間の外にある場合: 空配列とし、`answer` にその旨（真の0件ではないこと）を明記する
  - 各要素は `id` に加え、生成時点の `date` / `location` / `equipment` / `severity` と、
    ヒットした記述の抜粋（`excerpt`）を保持する。事故記録が後から更新・削除されても、
    回答生成時に参照した内容を過去の回答から再現できる
  - `excerpt` の抜粋元は検索でヒットした語が実際に含まれる欄（`description` / `cause` /
    `countermeasure`）を優先する。抜粋元の欄名は `excerpt_field` に持つ。ヒット箇所を
    特定できない場合（集計の回答、ヒットした語がどの欄にも見つからない場合）は
    `excerpt_field` は `null` になり、`description` の先頭にフォールバックする
- `basis` は回答の根拠情報（解釈済みの絞り込み条件・参照した件数と母数・記録の収録期間）。
  ツールを呼び出した問い合わせでは必須、呼び出していない場合は `null`（ADR 009）
  - 人間向けの要約は `answer` 本文の末尾にも決定的に付与される（`answer` だけを読む
    利用者にも解釈済み条件・見落とし・データの新旧が伝わるようにするため）
  - `matched_count` は絞り込み条件に合致した件数（絞り込みなしの検索では全記録数）、
    `truncated_count` は参照されなかった件数（集計は常に0。`citations` が母集団全件のため）
- エラーは RFC 9457 Problem Details（`application/problem+json`）で返す。標準メンバーに加えて機械可読な `code` と、ログ突合用の `trace_id` を含む
- `code` の語彙・エラー応答に使う HTTP ステータスは後方互換に拡張されうる。クライアントは
  未知の `code` / ステータスを汎用エラーとして扱うこと（破壊的変更に当たらない）
- API スキーマレイヤーとバックエンドレイヤーの依存は「スキーマ → バックエンド」の一方向とする。バックエンドは HTTP / Web フレームワークに依存しない
- フレームワーク既定のバリデーションエラー（422 等）は返さない。バリデーション失敗は 400 + Problem Details に統一する

# フェーズ1: 単一ホストでの軽量実装

## 実行前提

- LLM はローカル動作できる軽量モデル **GitHub Codespaces 標準スペック（4 コア・16GB RAM・GPU なし）で動作すること**が必須
- 信頼された環境での利用を前提とし、認証・レート制限は持たない（フェーズ2 で置換）

## 技術選定

- バックエンド言語: Python
- パッケージ管理: uv
- lint / format: ruff
- API アーキテクチャ: REST
- API スキーマ: OpenAPI 3.1（code-first。実装から生成し `openapi/openapi.yaml` としてリポジトリにコミットする）
- Web フレームワーク: FastAPI（ASGI / uvicorn）。実装は async、バリデーションは Pydantic v2
- テスト: pytest。API スキーマ/バックエンドレイヤーの依存方向検証: import-linter
- データストア: DuckDB。構造化データと埋め込みベクトルは同一ファイルで管理する
  （問い合わせ履歴の分離は「運用」節を参照）
- LLM 推論ランタイム: Ollama / 生成モデル: `hf.co/unsloth/Qwen3-4B-Instruct-2507-GGUF`（既定量子化 Q4_K_M）
- 埋め込みモデル: `cl-nagoya/ruri-v3-130m`（sentence-transformers 経由）
- 検索: BM25（`bm25s` + `fugashi`）とベクトル検索のハイブリッド（Reciprocal Rank Fusion で統合）
- 集計: LLM の function calling による定義済み集計関数群（Text-to-SQL は不採用）
- ログ: `structlog`。標準出力への JSON Lines 固定（ファイル出力・ローテーション・外部送信は持たない）。リクエストごとに発行する `trace_id` を全ログへ自動付与し、Problem Details の `trace_id` と突合できる。質問文・回答文はログに含めず、`query_id` で監査ログ（DuckDB）と突合する
- 回答品質の評価: 自前の評価ハーネス（`evals/`）+ リポジトリ内データセット。LLM-as-judge は不採用（判定側に LLM を使わず、参照値との決定的な突合で検証する）。検索精度（Ollama 不要）は毎 PR、ツール選択・回答生成に関わる変更を検知した場合は回答品質評価（Ollama 要）も自動実行する。捏造・偽ゼロ等の絶対要件は固定閾値で常時検証し、精度指標は直近実測値（ベースライン）からの低下のみを検出する

## 運用

- `data/accidents.csv` を正データとし、起動時 ETL で DuckDB へ取り込む（フェーズ2 で置換）。
  DuckDB 側は CSV から再構築可能なキャッシュと位置づけ、リポジトリにコミットしない
- 事故記録キャッシュ（再構築可能）と問い合わせ履歴（監査ログ）は別ファイルに分離する
- 検索インデックス・埋め込みモデルはアプリケーションプロセス内に保持する（フェーズ2 で置換）
- 設定は環境変数（`SAFETY_QA_` プレフィックス）で与える

# フェーズ2: クラウド運用（要件）

## 移行の原則

- API 契約を変更せずに移行できること
- 実装差し替えの範囲はバックエンドレイヤー内の実装詳細に限り、`core/ports.py` に定義した
  各ポート（リポジトリ・検索・推論等）のシグネチャ変更を要さないこと
- API スキーマレイヤー→バックエンドレイヤーの一方向依存の契約を維持し、機械的検証を継続すること
- 実行環境ごとの差異は設定で吸収し、アプリケーションコードの分岐を持たないこと

## 実行基盤

- コンテナイメージとして構築・配布し、実行基盤に依存しない起動手順を持つこと
- 複数インスタンスを並行稼働させ、水平スケールできること。そのためインスタンスはステートレスであり、リクエスト処理に必要な状態をローカルファイルシステムに置かないこと
- 起動時に実行しているデータ取り込み（ETL）を、リクエスト処理から分離して実行できること
- 秘匿情報を含まない設定は環境変数で与え、イメージの再ビルドなしに変更できること

## データ

- 事故記録・埋め込みベクトル・問い合わせ履歴を、複数インスタンスから同時に読み書きできるデータストアで保持すること
- 検索対象データ量の増加に対し、全件走査に依存しない近似最近傍探索へ移行できること
- 正データの更新を、手動配置に依らない経路で取り込めること。取り込みは増分で行い、変更のないレコードの再埋め込みを行わないこと
- 問い合わせ履歴（監査ログ）は事故記録データと保持期間・アクセス制御を独立に管理できること

## LLM 推論

- 推論をアプリケーションプロセスから分離し、独立にスケール・差し替えできること
- 生成モデル・埋め込みモデルを、アプリケーションコードの変更なしに設定で切り替えられること
- 推論の同時実行数上限・タイムアウトを、実行基盤上の実測に基づいて設定できること

## 回答品質の評価

- 回答品質の評価に、被評価モデルと独立した上位モデルを judge として使えること
- judge のスコアは合否判定ではなく傾向監視に使い、フェーズ1で確立した決定的な絶対要件（捏造・偽ゼロ等の検証）は維持すること
- judge の判定を人手評価のサンプルと突合し、judge 自体の妥当性を継続的に検証できること
- 評価データセットに、プロンプト調整に用いていない記録を含められること（過適合の検知）

## 可観測性

- ログは標準出力への JSON Lines のまま維持し、収集・保存・保持期間の管理は実行基盤の責務とする（アプリケーションが送信先を持たない）
- `trace_id` を分散トレーシングの基盤と接続できること。信頼できるプロキシ配下では受信ヘッダからの `trace_id` 継承を有効にできる
- ログに質問文・回答文を含めず、`query_id` / `trace_id` で監査ログと突合できる状態を維持する
- リクエスト数・レイテンシ分布・エラー率等のメトリクスを収集できること

## 公開 API としての要件

- 利用者を識別する認証方式を持つこと
- 利用者単位のレート制限を持ち、超過時は既定のエラー形式（`RATE_LIMITED` / 429）で応答すること
- 秘匿情報（認証情報・モデル API の資格情報等）を、環境変数への直接埋め込みに依らない方式で管理すること
- 通信経路および保存データを暗号化すること
- 監査ログへのアクセスを、アプリケーション運用者の権限とは独立に制御できること

## 可用性・耐障害性・デプロイ

- 単一の障害点を持たず、複数の可用性ゾーンにまたがって稼働できること
- 稼働状態を判定するヘルスチェック経路を持ち、異常なインスタンスを自動的に切り離せること
- 依存サービス（推論・データストア）の障害時に、原因を含むエラー応答を返して縮退すること。障害を無言で握り潰さない
- 新バージョンの配置を無停止で行い、問題時に直前のバージョンへ戻せること

## 非機能目標の扱い

- レイテンシ・スループット・可用性の数値目標は、実行基盤上での実測に基づいて別途決定する。本仕様では数値を規定しない
